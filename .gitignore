# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# Large data files — store externally (Globus / local copy only) unless tracked below
data/SNR_Grid_IMRPhenomPv2_FD_all_noise.hdf5
data/sspc/
data/*.tar.gz

# BPS input: allow one committed copy for Step 00 (use Git LFS if > ~100 MB; GitHub hard limit 100 MB per file).
# If your file is named *.hdf5, rename or symlink to data/bps_output.h5 or pass --bps-hdf5 to 00_sspc_data_generation.py
data/*.hdf5
!data/bps_output.hdf5
data/*.h5
!data/bps_output.h5

# Generated ML artifacts (any path under checkpoints/, including ensemble subdirs)
checkpoints/

# Generated datasets / tables from Step 02 (repo root or data/)
*.parquet
hyperparam_table.csv
hyperparam_table_encoded.csv
splits.json
obs_normalizer.json

data/*.parquet
data/*.csv
!data/gwtc_sample_events.csv
data/*.json

# SLURM logs
logs/
*.out
*.err

# Plots and validation outputs (regenerate on cluster or laptop)
plots/
test/reports/
test/plots/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
